import { StyleSheet } from 'react-native';

const backgroundColor = '#EE7777';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor,
  },
  containerPosts: {
    flex: 1,
    backgroundColor,
    textAlign: 'center',
    justifyContent: 'center',
  },
});

export default styles;
